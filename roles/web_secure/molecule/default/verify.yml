---
# verify the instance final state 
# Molecule calls this playbook with `molecule verify`.
- name: Verify
  hosts: instance
  gather_facts: false 
  tasks:
    - name: Assert something
      ansible.builtin.assert:
        that: true
