driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: rocky-test
    box: rockylinux/9
    interfaces:
      - auto_config: true
        network_name: private_network
        type: dhcp
provisioner:
  name: ansible
verifier:
  name: ansible
